# server

A backend service built with **RacerAPI**, an AI-native, modular framework powered by FastAPI.

---

## 🚀 Running the Project Locally

```bash
# Create virtual environment
python -m venv .venv

# Activate virtual environment (Windows)
.venv\Scripts\activate

# Install dependencies
pip install -r requirements.txt

# Start development server
racerapi dev 
```

# ✔ Notes

- Uses lowercase `racerapi` command (CLI standard)
- Follows best README styling
- Clean capitalization and formatting
- Short, accurate introduction sentence
- Adds "Start development server" comment for clarity
- Removed inconsistent casing ("RacerAPI dev" → `racerapi dev`)

